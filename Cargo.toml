[workspace]
members = [
    "crates/app",
    "crates/core",
    "crates/infra",
    "crates/drivers",
    "crates/telemetry-collector",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
strip = "symbols"
