[package]
name = "oqqwall_rust_infra"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = "1.3.3"
crc32fast = "1.4.2"
oqqwall_rust_core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
